<%= simple_form_for(@activity) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :activityType %>
    <%= f.input :tcx %>

  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
